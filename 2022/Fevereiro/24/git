## Git

Git stash - para redirecionar os arquivos ques estao no staged da branch errada
git stash file
git stash , git checkout para branch certa, git stash apply

Git tag - Marcar um ponto importante da branch
$ git tag -a <nome_da_tag> -m <mensagem>
$ git push origin <nome_da_tag>
git show <nome_da_tag>

$ git branch -d nome_da_branch- deletar uma branch local
$ git push origin --delete nome_do_branch_remota - deletar uma branch remota

